name: kumoriya_manga_domain
description: Manga/manhwa/manhua domain models and use-case-facing contracts.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.1

dependencies:
  kumoriya_core:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
