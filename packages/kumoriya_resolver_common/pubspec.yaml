name: kumoriya_resolver_common
description: Shared utilities for Kumoriya resolver plugins (Dean-Edwards unpacking, payload normalization, URL helpers).
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.1

dependencies:
  http: ^1.3.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
