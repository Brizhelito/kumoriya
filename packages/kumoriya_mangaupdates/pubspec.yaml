name: kumoriya_mangaupdates
description: MangaUpdates metadata gateway. Provides series search, group activity status, and release timelines used by the manga catalog picker enrichment (M8) and as a fallback resolver when MangaBaka does not cover a series.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.1

dependencies:
  http: ^1.5.0
  kumoriya_core:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
