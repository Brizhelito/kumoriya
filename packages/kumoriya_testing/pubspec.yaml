name: kumoriya_testing
description: Shared testing helpers for Kumoriya packages.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.1

dependencies:
  test: ^1.25.6

dev_dependencies:
  lints: ^6.0.0
