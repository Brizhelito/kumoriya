name: kumoriya_matching
description: Production-grade entity resolution and canonical source mapping for Kumoriya.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.1

dependencies:
  kumoriya_domain:
  kumoriya_plugins:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
