name: kumoriya_mangabaka
description: MangaBaka metadata gateway. Provides title-corpus expansion and cross-tracker IDs (AniList, MAL, MangaUpdates, Kitsu, AnimePlanet) used by the manga catalog matching pipeline.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.1

dependencies:
  http: ^1.5.0
  kumoriya_core:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
