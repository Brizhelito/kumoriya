## 0.1.0

- Initial scaffolding: `MangaSourcePlugin` contract, source-side models
  (`SourceMangaMatch`, `SourceMangaDetail`, `SourceChapter`, `SourcePage`),
  request value objects, and `MangaSourceCapabilities` feature flags.
