# Dev diary — 2026-04-28

## 09:35 — Manga phase Slice 4: `kumoriya_manga_plugins` package

**Category:** feature
**Affected files:**
- `pubspec.yaml`
- `packages/kumoriya_manga_plugins/pubspec.yaml`
- `packages/kumoriya_manga_plugins/analysis_options.yaml`
- `packages/kumoriya_manga_plugins/.gitignore`
- `packages/kumoriya_manga_plugins/CHANGELOG.md`
- `packages/kumoriya_manga_plugins/README.md`
- `packages/kumoriya_manga_plugins/lib/kumoriya_manga_plugins.dart`
- `packages/kumoriya_manga_plugins/lib/src/contracts/manga_source_plugin.dart`
- `packages/kumoriya_manga_plugins/lib/src/models/manga_chapter_query.dart`
- `packages/kumoriya_manga_plugins/lib/src/models/manga_search_query.dart`
- `packages/kumoriya_manga_plugins/lib/src/models/manga_source_capabilities.dart`
- `packages/kumoriya_manga_plugins/lib/src/models/source_chapter.dart`
- `packages/kumoriya_manga_plugins/lib/src/models/source_manga_detail.dart`
- `packages/kumoriya_manga_plugins/lib/src/models/source_manga_match.dart`
- `packages/kumoriya_manga_plugins/lib/src/models/source_page.dart`
- `packages/kumoriya_manga_plugins/test/manga_source_plugin_contract_test.dart`

**Summary:**
New pure-Dart package `kumoriya_manga_plugins`, parallel to `kumoriya_plugins`. Defines:

- `MangaSourcePlugin` — async contract over `Result<T, KumoriyaError>` with `search`, `getLatestUpdates`, `getMangaDetail`, `getChapters`, `getChapterPages`. Plugins that don't support a feature (e.g. latest feed) MUST return an empty `Success`, not a failure.
- Source-side models: `SourceMangaMatch` (light search row, AniList alignment fields), `SourceMangaDetail` (synopsis/authors/artists/tags/status/originalLanguage), `SourceChapter` (`double number` for 12.5/side-stories, optional `volume`/`language`/`scanlator`/`publishedAt`/`pageCount`), `SourcePage` (`index >= 0`, direct `imageUrl`, optional per-request `headers`, optional `width`/`height`).
- Request value objects: `MangaSearchQuery` and `MangaChapterQuery` with 1-indexed pages and asserted invariants.
- `MangaSourceCapabilities` — a separate value type carrying per-plugin feature flags (`supportsLanguageFilter`, `supportsScanlatorFilter`, `supportsLatestFeed`, `requiresPageHeaders`). Kept off `PluginCapability` to avoid polluting the anime-flavored enum; manga flags are decision-axes anime sources never need.

Reuses `PluginManifest` and `PluginType` from `kumoriya_plugins` as-is — no fork. Depends only on `kumoriya_core`, `kumoriya_manga_domain`, `kumoriya_plugins`. Zero coupling to a concrete source, the reader, or storage.

Workspace `pubspec.yaml` registers the new package. Tests cover capability defaults, value equality, query-invariant asserts, fractional chapter numbers, and a fake plugin satisfying the full interface. `dart format` clean, `dart analyze` 0 issues on the new package and across the workspace, 8/8 contract tests pass.

**Residual risk:** the package is dead code until Slice 6 (`kumoriya_source_mangadex`). The contract surface — particularly chapter `language`/`scanlator` shape and `SourcePage.headers` — is the most likely thing to need a reshape once a real source hits it; intentionally kept minimal so that reshape is cheap.

## 10:05 — Manga phase Slice 5: `kumoriya_storage` manga tables (schema v20)

**Category:** feature
**Affected files:**
- `packages/kumoriya_storage/lib/src/drift/app_database.dart`
- `packages/kumoriya_storage/lib/src/drift/app_database.g.dart` (regenerated)
- `packages/kumoriya_storage/lib/src/drift/tables/manga_cache_table.dart`
- `packages/kumoriya_storage/lib/src/drift/tables/manga_chapter_table.dart`
- `packages/kumoriya_storage/lib/src/drift/tables/manga_progress_table.dart`
- `packages/kumoriya_storage/lib/src/drift/tables/manga_history_table.dart`
- `packages/kumoriya_storage/lib/src/drift/tables/manga_library_table.dart`
- `packages/kumoriya_storage/lib/src/drift/tables/chapter_page_cache_table.dart`
- `packages/kumoriya_storage/lib/src/drift/tables/manga_download_table.dart`
- `packages/kumoriya_storage/lib/src/drift/daos/manga_cache_dao.dart` (+`.g.dart`)
- `packages/kumoriya_storage/lib/src/drift/daos/manga_chapter_dao.dart` (+`.g.dart`)
- `packages/kumoriya_storage/lib/src/drift/daos/manga_progress_dao.dart` (+`.g.dart`)
- `packages/kumoriya_storage/lib/src/drift/daos/manga_library_dao.dart` (+`.g.dart`)
- `packages/kumoriya_storage/lib/src/drift/daos/chapter_page_cache_dao.dart` (+`.g.dart`)
- `packages/kumoriya_storage/lib/src/drift/daos/manga_download_dao.dart` (+`.g.dart`)
- `packages/kumoriya_storage/lib/src/contracts/manga_cache_store.dart`
- `packages/kumoriya_storage/lib/src/contracts/manga_chapter_cache_store.dart`
- `packages/kumoriya_storage/lib/src/contracts/manga_progress_store.dart`
- `packages/kumoriya_storage/lib/src/contracts/manga_library_store.dart`
- `packages/kumoriya_storage/lib/src/contracts/chapter_page_cache_store.dart`
- `packages/kumoriya_storage/lib/src/contracts/manga_download_store.dart`
- `packages/kumoriya_storage/lib/src/drift/drift_manga_cache_store.dart`
- `packages/kumoriya_storage/lib/src/drift/drift_manga_chapter_cache_store.dart`
- `packages/kumoriya_storage/lib/src/drift/drift_manga_progress_store.dart`
- `packages/kumoriya_storage/lib/src/drift/drift_manga_library_store.dart`
- `packages/kumoriya_storage/lib/src/drift/drift_chapter_page_cache_store.dart`
- `packages/kumoriya_storage/lib/src/drift/drift_manga_download_store.dart`
- `packages/kumoriya_storage/lib/kumoriya_storage.dart`
- `packages/kumoriya_storage/test/app_database_migration_test.dart`
- `packages/kumoriya_storage/test/drift_manga_cache_store_test.dart`
- `packages/kumoriya_storage/test/drift_manga_chapter_cache_store_test.dart`
- `packages/kumoriya_storage/test/drift_manga_progress_store_test.dart`
- `packages/kumoriya_storage/test/drift_manga_library_store_test.dart`
- `packages/kumoriya_storage/test/drift_chapter_page_cache_store_test.dart`
- `packages/kumoriya_storage/test/drift_manga_download_store_test.dart`

**Summary:**
Schema bump 19 → 20 with seven strictly-additive tables for the manga universe: `manga_cache`, `manga_chapter`, `manga_progress`, `manga_history`, `manga_library`, `chapter_page_cache`, `manga_download`. Anime tables and DAOs untouched (asserted by the new v19→v20 regression test). Each table has a typed Drift definition (`REAL` chapter numbers preserve `12.5`/side-stories, `Map<String,String>` HTTP headers serialised as JSON via `dart:convert`), a DAO, a contract returning `Result<T, KumoriyaError>`, and a `Drift*Store` implementation. Mirrors the existing anime pattern (`AnilistCacheStore`/`LibraryStore`/`AnimeProgressStore`/`DownloadStore`) with manga-specific differences: `MangaCacheStore` carries `countryOfOrigin`, `originalLanguage`, `totalChapters`, `totalVolumes`; `MangaChapterCacheStore` is keyed on `(sourceId, sourceChapterId)` so the same chapter from a different scanlator/language is a different row; `ChapterPageCacheStore` indexes the on-disk page cache (bytes live on disk, this table holds metadata + TTL); `MangaDownloadStore` adds a `partial` status for chapters that downloaded page-by-page but failed to package into a CBZ.

`MangaCacheStore.upsert` mirrors the anime cache trick: when `relationsJson == null` the column is left untouched (`Value.absent()`) so list-level refreshes don't NULL-out relations cached by an earlier detail-level fetch. Library "soft-delete" mirrors the anime pattern too: removing a favorite leaves the row when `notify`/`autoDownload`/`preferredLanguage`/`preferredScanlator` are still set.

Eleven new partial/composite indices, all prefixed `idx_manga_*` to avoid collisions. Migration step at `from < 20` invokes `_createTableIfMissing` per table and `_createMangaIndices()`. `beforeOpen` for fresh DBs now also calls `_createMangaIndices()`.

**Verification:** `dart format` clean, `dart analyze` 0 issues at package and workspace level, `dart test` 105/105 pass including the new v19→v20 migration regression test (asserts manga tables + indices are created and anime table SQL is byte-identical post-migration). Drift codegen ran clean (`build_runner build --delete-conflicting-outputs`).

**Other fix:** the existing `app_database_migration_test.dart` asserted `user_version == 18`, drifted from the live `19`. Bumped to `20` alongside this slice.

**Residual risk:** the manga storage layer is dead code until app-layer composition (Slice 6 `kumoriya_source_mangadex` for `manga_chapter`/`manga_cache` writes; Slice 11 `kumoriya_offline_manga` for `manga_download`/`chapter_page_cache`). The two contract surfaces most likely to reshape are (a) `MangaChapterCacheStore.replaceForManga` semantics once a source plugin's chapter pagination is real, and (b) the `MangaDownloadStatus.partial` state's interaction with retry semantics — chosen now to keep CBZ-failed chapters resumable, but its boundary with `failed` is a product call that may move.
