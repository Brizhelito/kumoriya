# 2026-04-18 — AniList Home edge-cache + FCM notifications backend

## Summary

Delivered the backend half of the AniList Home edge-cache and FCM airing
notifications pipeline. No Flutter-side changes yet; clients continue to
call AniList directly until Slice 2 lands.

## What shipped

### Slice 1 — AniList Home cache (Go)

Centralised three high-reuse Home surfaces (trending, season-discovery,
airing-calendar) behind an in-memory stale-while-revalidate cache.
Clients that opt in (Slice 2, pending) hit the API instead of
`graphql.anilist.co`, which keeps the tiny AniList rate limit budget
from being multiplied by thousands of users.

Per-media detail, search, lists and relations remain client-direct on
purpose: they are long-tail and benefit from distributed IPs, not from
central caching.

- `internal/anilist/` — queries mirror `packages/kumoriya_anilist`
- `internal/anilist/client/` — minimal GraphQL client
- `internal/anilist/cache/` — SWR cache with single-flight refresh and
  "fallback to stale on loader failure" behaviour so transient AniList
  outages stay invisible to users
- `internal/anilist/service/` — HomeService (Trending, SeasonDiscovery,
  AiringCalendar) with conservative TTLs (10 min / 30 min / 5 min fresh)
- `internal/anilist/scheduler/` — prewarm cron that runs immediately at
  boot and on cadence
- `internal/anilist/handler/` — three public REST endpoints under
  `/v1/anilist/home/*` with `Cache-Control` + `X-Cache-Stale`/`X-Cache-Age`
  observability headers

Tests: 5 unit tests for the SWR cache (cold miss, stale refresh, expired
fallback, cold-miss failure propagation, single-flight), 6 unit tests
for HomeService, and 4 integration tests against real AniList.

### Slice 5 — FCM airing notifications + Upstash dedup (Go)

Backend of the airing notifications pipeline. The Flutter side of this
slice (topic subscribe/unsubscribe on library toggle, notification
permission, deep-link handling) is not yet done.

- `internal/redis/` — minimal Upstash REST client (PING, SETNX, DEL).
  REST chosen over TCP because the load is trivial (a few commands per
  airing cycle) and it works identically from Cloudflare Workers if we
  ever want cross-runtime sharing.
- `internal/notifications/dedup.go` — RedisDeduper with keys
  `notif:sent:media:{id}:ep:{n}` and 7-day TTL. Keeps restarts of the HF
  Space from re-notifying users.
- `internal/notifications/fcm_sender.go` — wraps
  `firebase.google.com/go/v4` Messaging. Supports both env-var JSON
  (preferred for HF Spaces) and file-path credentials.
- `internal/notifications/airing_worker.go` — polls the cached calendar,
  filters to the "just aired" window, claims `(mediaID, episode)` in
  Redis, dispatches one FCM message per claim to `media_{anilist_id}`
  topics. Adult media is filtered. FCM failures on one media do not
  block dispatching the rest of the cycle.
- `cmd/server/main.go` — wires the worker behind a soft gate: if either
  Firebase or Upstash creds are missing, the worker stays disabled with
  a warning log so dev boots cleanly.

Tests: 6 unit tests (window filtering, dedup across 5 cycles, adult
skip, partial FCM failure tolerance, title fallbacks, empty payload,
service-account JSON validator) and integration tests against real
Upstash (PING + SETNX) and Firebase Admin SDK (sender constructs).

## Key decisions

- **AniList data split** — shared surfaces centralised, personalised
  surfaces stay client-direct. This keeps our server's AniList rate
  limit usage constant regardless of user count.
- **Memory-only Home cache** — Upstash was proposed for Home but the
  blast radius of a cache reset on HF restart is tiny (~4 refetches to
  warm up). Memory avoids a dependency we do not need.
- **Redis used only for notification dedup** — this is the one piece of
  state that, if lost on restart, causes a user-visible bug (duplicate
  pushes). Everything else is recoverable.
- **Subscriptions reuse existing `LibraryEntry.notify_new_episodes`** —
  no new tables, no new sync endpoints. Discovered the data model
  already supports this.
- **FCM topics, not user-token fan-out** — eliminates the need for the
  server to keep an authoritative `media -> users` index.

## Security / infra

- `.gitignore` now excludes `*.env`, `google-services*.json`,
  `firebase-adminsdk*.json`, `firebase-service-account*.json`.
- `google-services.json` moved to
  `apps/kumoriya_app/android/app/google-services.json`. The file
  registers both `dev.kumoriya.app` and `dev.kumoriya.app.debug` in one
  artifact; the Gradle plugin picks the right client per variant.
- HF Spaces secrets required for Slice 5 runtime:
  - `UPSTASH_REDIS_REST_URL`
  - `UPSTASH_REDIS_REST_TOKEN`
  - `FIREBASE_SERVICE_ACCOUNT_JSON` (raw JSON content)

## Validation

- `go build ./...` ✅
- `go vet ./...` ✅
- `go test ./...` ✅ (all packages, unit)
- `go test -tags=integration ./internal/anilist/...` ✅ (real AniList)
- `go test -tags=integration ./internal/redis/...` ✅ (real Upstash)
- `go test -tags=integration ./internal/notifications/...` ✅ (real
  Firebase Admin SDK construction)
- `gofmt -l` clean

## Residual risk

- The airing worker has never sent a push to a real device yet.
  Slice 3+4 (Flutter FCM setup, topic subscribe on ♥) is required
  before end-to-end dispatch can be observed.
- `media_{id}` topic discipline assumes clients call
  `FirebaseMessaging.subscribeToTopic('media_{id}')` when
  `notify_new_episodes` flips to true. If Slice 4 forgets this, the
  server will dispatch into empty topics (no user impact, just no
  notifications).
- HomeService cache TTLs are first guesses. Real traffic will tell us
  if `trending` at 10-min fresh is too aggressive. Easy to tune.

## Later the same day — Slices 2, 4, 6 shipped

### Slice 4 — FCM topic subscriptions wired to library toggle

- `FcmAwareLibraryStore` decorator: every `setSubscription(id, notify:)`
  now mirrors to `FirebaseMessaging.subscribeToTopic('media_{id}')` /
  `unsubscribeFromTopic(...)`. Inner write failures short-circuit; FCM
  errors are swallowed so the library mutation always succeeds.
- `FcmTopicSyncService.syncTopicsWithLibrary()`: idempotent
  reconciliation invoked (a) after login migration+pull completes and
  (b) on every app boot from `AuthStateNotifier.build()` when a session
  is already active. Local→FCM only — FCM does not expose "list my
  topics", so stale subscriptions on their side are harmless and
  re-subscribing is a no-op.
- Tests: 6 for `fcm_topic.dart` (topic naming / parsing), 6 for
  `FcmAwareLibraryStore` (toggle, inner-write failure, FCM error
  swallowing, pass-through).

### Slice 2 — Backend-first AniList gateway + feature flag

- `AnilistHomeBackendClient`: public unauthenticated HTTP client for
  `/v1/anilist/home/{trending,season-discovery}` with a short
  per-request timeout. Maps network / HTTP / JSON errors into
  `AnilistError` values so the decorator can decide to fall back.
- `BackendFirstAnilistMetadataGateway`: overrides `fetchHomeCatalog`
  and `fetchSeasonDiscovery`; **everything else passes through
  unchanged** (including airing calendar — contract mismatch with the
  Go endpoint's `days` parameter vs Dart's `from`/`to` timestamps is
  a follow-up slice).
- Fallback behaviour: on any backend error (transport, non-2xx,
  malformed payload), transparently calls the inner direct-AniList
  gateway. Users never see an error caused by the Kumoriya backend
  being unavailable.
- Feature flag: `KUMORIYA_GO_ANILIST_HOME` (default true) via
  `--dart-define` for emergency rollback without rebuild of the
  backend.
- `KumoriyaRuntimeConfig.apiBaseUrl` exposes the Go backend URL so
  it can be overridden at build time via
  `KUMORIYA_API_BASE_URL`.
- Tests: 8 unit tests — success path, 502/malformed/network-error
  fallback for trending, three variants for season discovery, and a
  pass-through assertion for airing calendar (must never hit the
  backend).

### Slice 6 — Legacy worker trimmed, not deleted

**User flagged a real concern**: blindly removing the worker would
also kill auto-download of new episodes. The worker does two jobs —
local notifications (now redundant with FCM) AND auto-download
(still client-side, still needed).

Decision after discussion: **keep the worker** but reduce its
responsibilities.

- `check_new_episodes_worker.dart` rewritten: removed all
  `flutter_local_notifications` code paths, the
  `kCheckNewEpisodesDebugProbeTask` branch, and the
  `_runDebugProbe` / `_initNotifications` / `_sendNotification` /
  `_formatNotificationBody` helpers. 516 lines → 406 lines. Pure
  auto-download + warmup now.
- `main.dart`: `_initWorkmanager` cadence dropped from 1h to 4h.
  The cancel-on-FCM block was removed — the worker must keep
  running for auto-download. Comment updated.
- `settings_page.dart`: removed the debug "Test notificacion"
  button and `scheduleDebugBackgroundNotificationProbe` import.
- Channel-ID alignment bug fixed: server was sending
  `airing_episodes` while the Android client created
  `kumoriya_new_episodes`. Pushes would have landed in an
  auto-created channel with default importance instead of the
  configured one. Server now sends `kumoriya_new_episodes`.

**Why 4h and not FCM-driven retries?** Two reasons:
1. Sources publish episodes 30 min – 24 h after AniList airing time.
   An FCM-driven immediate attempt would almost always fail on fast
   sources. A state-machine of retries (30 min / 2 h / 6 h) was
   discussed but is too much complexity for this slice.
2. 4h worker cadence covers the entire source-publish window with
   ≤4h of extra latency past source availability. User's push
   notification is still instant; auto-download tolerates the gap.

Known limitation: N devices × 1 scrape per 4h still multiplies at
the source when many users track the same airing anime. Documented
as a future slice ("collaborative availability cache") in
`docs/architecture/notifications_and_auto_download.md`.

### Validation

- `flutter analyze` (all modified areas): clean.
- `flutter test test/fcm_topic_test.dart
  test/fcm_aware_library_store_test.dart
  test/backend_first_anilist_gateway_test.dart`:
  20/20 passing.
- `go build ./...`: clean.

## 16:40 — Watch Party navigation mode split

**Category:** feature
**Affected files:**
- `apps/kumoriya_app/lib/src/features/anime_catalog/presentation/pages/anime_detail_page.dart`
- `apps/kumoriya_app/lib/src/features/anime_catalog/presentation/pages/episode_list_page.dart`
- `apps/kumoriya_app/lib/src/features/anime_catalog/presentation/support/playback_launch_flow.dart`
- `apps/kumoriya_app/lib/src/features/player/presentation/pages/player_page.dart`
- `apps/kumoriya_app/lib/src/features/watch_party/presentation/pages/party_lobby_page.dart`
- `apps/kumoriya_app/lib/src/features/watch_party/presentation/party_route_mode.dart`

**Summary:**
Introduced a lightweight party presentation mode so Watch Party browse/player routes can live above the app shell without duplicating Anime Detail or Episode List. Party lobby/media-change navigation now opens party-aware detail screens on the root navigator, episode/player launches preserve that mode, and exiting the player returns to the lobby instead of unwinding the global stack. Validation was clean for targeted `dart analyze` and `watch_party_session_guard_test`; the existing `anime_detail_episode_integration_test.dart` is still blocked by a pre-existing `AnimeProgressStore.clearAllProgress` fake mismatch.

**Linked phase:** Phase 3.0

---

## 23:07 — Player adjacent episode fallback + offline preference hardening

**Category:** fix
**Affected files:**
- `apps/kumoriya_app/lib/src/features/anime_catalog/application/use_cases/start_episode_playback_use_case.dart`
- `apps/kumoriya_app/lib/src/features/player/presentation/pages/player_page.dart`
- `apps/kumoriya_app/lib/src/features/anime_catalog/presentation/pages/home_page.dart`
- `apps/kumoriya_app/lib/src/features/anime_catalog/presentation/pages/anime_detail_page.dart`
- `apps/kumoriya_app/lib/src/features/anime_catalog/presentation/pages/episode_list_page.dart`
- `apps/kumoriya_app/lib/src/features/anime_catalog/presentation/pages/downloads_page.dart`
- `apps/kumoriya_app/lib/src/features/watch_party/presentation/pages/party_anime_page.dart`
- `apps/kumoriya_app/lib/src/features/watch_party/presentation/pages/party_episode_list_page.dart`
- `apps/kumoriya_app/test/start_episode_playback_use_case_test.dart`

**Summary:**
Added an interactive-adjacent playback path so `next/prev` no longer burns time auto-resolving stale remembered servers before moving episodes; when multiple options exist it now falls back straight to server choice or focused episode fallback. Offline playback launches now opt out of durable playback preference persistence across home/detail/episode list/download/party entry points, which prevents deleted or one-off downloaded episodes from poisoning later streaming decisions. Party host episode changes from the party episode board now continue into the selected episode flow instead of bouncing back to lobby after only mutating room state. Targeted analyze passed with only existing `use_build_context_synchronously` infos in `player_page.dart`, and `flutter test apps/kumoriya_app/test/start_episode_playback_use_case_test.dart` passed.

**Linked phase:** Phase 3.0

---

## 17:08 — Sync timestamps hardened + OAuth state made stateless

**Category:** fix
**Affected files:**
- `apps/kumoriya_app/lib/src/shared/storage_providers.dart`
- `apps/kumoriya_app/lib/src/shared/sync/sync_providers.dart`
- `apps/kumoriya_app/lib/src/shared/sync/sync_refresh.dart`
- `apps/kumoriya_app/lib/src/shared/auth/auth_providers.dart`
- `apps/kumoriya_app/lib/src/shared/sync/anonymous_to_auth_migration.dart`
- `apps/kumoriya_app/lib/src/features/downloads/presentation/download_providers.dart`
- `packages/kumoriya_sync/lib/src/contracts/sync_service.dart`
- `packages/kumoriya_sync/lib/src/impl/http_sync_service.dart`
- `packages/kumoriya_sync/test/http_sync_service_test.dart`
- `kumoriya-api/internal/service/jwt_service.go`
- `kumoriya-api/internal/handler/auth_handler.go`
- `kumoriya-api/internal/handler/auth_handler_test.go`
- `kumoriya-api/cmd/server/main.go`

**Summary:**
Stopped the client sync pipeline from requeueing pulled server data by wiring `HttpSyncService` to raw stores instead of sync-aware wrappers, restored persisted `lastSyncAt` before each full sync, and now persist the real server sync time instead of overwriting it with `DateTime.now()`. Reintroduced a central Riverpod refresh pulse for sync/login/logout-adjacent data providers and preserved watch-history timestamps during anonymous-to-auth migration. On the backend, replaced the in-memory OAuth `state` cache with a signed stateless token so Google/Discord callbacks no longer depend on process-local memory; targeted validation passed with `dart analyze` on touched app files, `dart analyze packages/kumoriya_sync`, `dart test packages/kumoriya_sync/test/http_sync_service_test.dart`, and `go test ./internal/handler`. Residual risk: library-entry `added_at` on locally generated sync payloads still deserves a follow-up audit because some local non-favorite library mutations do not carry the original added timestamp.

**Linked phase:** Phase 3.0

---

## 18:40 — Client-owned sync queue replay + history deletions

**Category:** fix
**Affected files:**
- `packages/kumoriya_sync/lib/src/drift/drift_sync_queue_store.dart`
- `packages/kumoriya_sync/lib/src/impl/http_sync_service.dart`
- `packages/kumoriya_sync/lib/src/models/sync_status.dart`
- `packages/kumoriya_sync/test/http_sync_service_test.dart`
- `packages/kumoriya_storage/lib/src/contracts/library_store.dart`
- `packages/kumoriya_storage/lib/src/drift/daos/library_entry_dao.dart`
- `packages/kumoriya_storage/lib/src/drift/drift_library_store.dart`
- `apps/kumoriya_app/lib/src/shared/sync/sync_aware_progress_store.dart`
- `apps/kumoriya_app/lib/src/shared/sync/sync_aware_library_store.dart`
- `apps/kumoriya_app/lib/src/shared/sync/anonymous_to_auth_migration.dart`
- `apps/kumoriya_app/lib/src/shared/notifications/fcm_aware_library_store.dart`
- `apps/kumoriya_app/test/auto_download_new_episodes_service_test.dart`
- `apps/kumoriya_app/test/fcm_aware_library_store_test.dart`

**Summary:**
Aligned sync with the intended architecture where the client remains the durable source of truth: successful `/sync/push` responses now return queue entries to `pending` instead of clearing them, queue writes upsert by entity key for replay safety, and watch-history deletions are now enqueued and sent via `watch_history_deletions`. Library sync payloads now read from real local entry snapshots so existing favorites preserve their stored `added_at` instead of always synthesizing a fresh timestamp; tracking-only rows still fall back to a synthetic positive timestamp because the current backend `library_entry` protocol cannot represent `added_at = 0` safely without a deeper schema/merge change. Targeted `dart analyze` passed and `dart test packages/kumoriya_sync/test/http_sync_service_test.dart` passed; targeted `flutter test` remained blocked by the existing local `flutter_tester`/`sqlite3` native-asset issue in this environment.

**Linked phase:** Phase 3.0

---

## 17:20 — Party pages split from anime detail pages

**Category:** refactor
**Affected files:**
- `apps/kumoriya_app/lib/src/features/anime_catalog/presentation/pages/anime_detail_page.dart`
- `apps/kumoriya_app/lib/src/features/anime_catalog/presentation/pages/episode_list_page.dart`
- `apps/kumoriya_app/lib/src/features/player/presentation/pages/player_page.dart`
- `apps/kumoriya_app/lib/src/features/watch_party/presentation/pages/party_anime_page.dart`
- `apps/kumoriya_app/lib/src/features/watch_party/presentation/pages/party_episode_list_page.dart`
- `apps/kumoriya_app/lib/src/features/watch_party/presentation/pages/party_lobby_page.dart`

**Summary:**
Followed up on the initial navigation split by separating Watch Party into dedicated pages instead of driving party UX through `AnimeDetailPage` and `EpisodeListPage` constructors. `AnimeDetailScene` and `EpisodeListScene` now hold the shared widget logic, while `PartyAnimePage` and `PartyEpisodeListPage` provide party-only page identity and routing. Targeted analyze stayed clean and `watch_party_session_guard_test.dart` still passes.

**Linked phase:** Phase 3.0

---

## 18:05 — Party anime/episode pages got their own social UX

**Category:** feature
**Affected files:**
- `apps/kumoriya_app/lib/src/features/watch_party/presentation/pages/party_anime_page.dart`
- `apps/kumoriya_app/lib/src/features/watch_party/presentation/pages/party_episode_list_page.dart`

**Summary:**
Replaced the thin party wrappers with dedicated Watch Party surfaces. `PartyAnimePage` now prioritizes room context, members, readiness, room-ready sources, and next-step CTAs instead of reusing the normal detail layout with synopsis-heavy content. `PartyEpisodeListPage` now behaves like a shared queue board: hosts can set the room episode directly, members see the lock clearly, and the current room episode is framed as the collaborative launch point. Targeted analyze stayed clean and `watch_party_session_guard_test.dart` still passes.

**Linked phase:** Phase 3.0

---

## 19:10 — Release publish pipeline moved to API + Neon cache

**Category:** infra
**Affected files:**
- `kumoriya-api/migrations/010_app_releases.sql`
- `kumoriya-api/internal/model/release.go`
- `kumoriya-api/internal/repository/release_repo.go`
- `kumoriya-api/internal/service/release_service.go`
- `kumoriya-api/internal/handler/release_handler.go`
- `kumoriya-api/internal/handler/release_handler_test.go`
- `kumoriya-api/internal/config/config.go`
- `kumoriya-api/cmd/server/main.go`
- `scripts/windows/publish-r2-release.ps1`
- `scripts/windows/load-r2-credentials.ps1`
- `secrets/update_publish.credentials.env`

**Summary:**
Introduced a Neon-backed release metadata pipeline with in-memory cache and explicit publish invalidation. The API now serves `/releases/latest`, `/releases/feed`, `/releases/:tag`, and a bearer-protected `/internal/releases/publish` endpoint; `latest` still falls back to legacy `update.json` when the cache is cold. The Windows publish script now loads local R2 + update secrets, uploads release artifacts to R2, publishes metadata to the API, and only then uploads the legacy `update.json` for older clients. Backend validation passed with `go test ./internal/handler ./internal/service ./internal/repository` and `go test ./cmd/server` using `GOCACHE=/tmp/...`; PowerShell script execution could not be exercised in this Linux environment.

**Linked phase:** Phase 4.0

---

## 20:05 — v0.2.0 published to API + R2, website rebuilt from release feed

**Category:** release
**Affected files:**
- `apps/kumoriya_app/pubspec.yaml`
- `apps/kumoriya_app/lib/src/features/app_update/application/release_notes_catalog.dart`
- `CHANGELOG.md`
- `docs/releases/README.md`
- `docs/releases/es/v0.2.0.md`
- `docs/releases/en/v0.2.0.md`
- `releases/versions/v0.2.0/release.json`
- `releases/manifests/update.json`
- `/home/reny/Projects/Kumoriya-Website/src/data/releases.ts`
- `/home/reny/Projects/Kumoriya-Website/src/components/Hero.astro`
- `/home/reny/Projects/Kumoriya-Website/src/components/Navbar.astro`
- `/home/reny/Projects/Kumoriya-Website/src/components/Platforms.astro`
- `/home/reny/Projects/Kumoriya-Website/src/components/AlphaCTA.astro`
- `/home/reny/Projects/Kumoriya-Website/src/components/Changelog.astro`

**Summary:**
Prepared release docs and bumped the app to `0.2.0+6`, built the Android release APK, uploaded the Android artifact plus `release.json`, changelog markdown, and legacy `update.json` to R2, and published `v0.2.0` into the Neon-backed API feed as the latest Android release. The website was rebuilt and redeployed from the API-backed release feed; the fresh deploy is confirmed on `kumoriya-website.renymireles.workers.dev`, while the custom domain `kumoriya.online` still appeared to serve an older deployment at verification time. Validation covered `flutter build apk --release`, `flutter test test/app_update_service_test.dart` (still running at entry time; final status recorded in task closeout), API endpoint verification via live HTTP requests, and a successful Cloudflare Worker deploy.

**Linked phase:** Phase 4.0

---
