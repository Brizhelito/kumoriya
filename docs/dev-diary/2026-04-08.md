# Dev Diary — 2026-04-08

> Auto-maintained by dev-diary skill. One entry per approved change.

---

## 16:00 — Dev diary skill created

**Category:** infra
**Affected files:**
- `.agents/skills/dev-diary/SKILL.md`

**Summary:**
Created the dev-diary skill to document every approved change immediately. Each entry is committed right after approval to prevent knowledge loss across sessions.

**Linked phase:** Phase 0

---

## 16:10 — HTTP idle timeout reduced (60s → 5s)

**Category:** fix
**Affected files:**
- `apps/kumoriya_app/lib/src/features/downloads/application/download_manager_service.dart`

**Summary:**
Reduced `idleTimeout` from 60s to 5s in both `_createDownloadHttpClient` and `_createInsecureDownloadHttpClient`. This releases server connections promptly after download completion, preventing CDN connection pool exhaustion on sequential downloads.

**Linked phase:** Phase 1.3

---

## 16:10 — Android concurrent downloads reduced (3 → 2)

**Category:** fix
**Affected files:**
- `apps/kumoriya_app/lib/src/features/downloads/application/download_manager_service.dart`

**Summary:**
Changed `_defaultMaxConcurrentDownloads()` Android branch from 3 to 2. Improves stability by reducing memory pressure and network contention on mobile.

**Linked phase:** Phase 1.5

---

## 16:15 — Defensive gzip handling for downloads

**Category:** fix
**Affected files:**
- `apps/kumoriya_app/lib/src/features/downloads/application/download_manager_service.dart`

**Summary:**
Some servers send `Content-Encoding: gzip` despite our `Accept-Encoding: identity` header. With `autoUncompress = false`, raw gzip bytes would corrupt video files. Added detection of gzip response encoding and on-the-fly decompression via `gzip.decoder`. When gzip is detected, `knownTotalBytes` is reset to 0 (indeterminate) since Content-Length refers to compressed size.

**Linked phase:** Phase 1.4

---

## 16:20 — Retry policy hardened + anti-ping-pong

**Category:** fix
**Affected files:**
- `apps/kumoriya_app/lib/src/features/downloads/application/download_manager_service.dart`

**Summary:**
Default `maxRetryAttempts` reduced from 3 to 2 for faster failure resolution. Added `_triedServersPerTask` set to track which servers have been attempted per download task. In `_tryRecoverDownload`, if the link refresher returns a server that was already tried, recovery is aborted instead of ping-ponging between failing servers. All cleanup paths (cancel, resume, clear, retry, dispose) now also clear the tried-servers tracking.

**Linked phase:** Phase 1.6

---

## 16:30 — Cronet HTTP client for resolver plugins (Android)

**Category:** feature
**Affected files:**
- `apps/kumoriya_app/lib/src/features/anime_catalog/application/services/plugin_runtime_catalog.dart`
- `apps/kumoriya_app/lib/src/features/anime_catalog/presentation/providers/anime_catalog_providers.dart`
- `apps/kumoriya_app/lib/src/workers/check_new_episodes_worker.dart`

**Summary:**
All resolver plugins (except AnimeNexus which uses Dio) now receive a shared Cronet-backed `http.Client` on Android via constructor injection. This provides HTTP/2 support, better TLS negotiation, and lower latency for resolver page fetches. Non-Android platforms fall back to the standard dart `http.Client`. A `resolverHttpClientProvider` Riverpod provider manages the shared client lifecycle. The background worker also creates its own Cronet client for resolver use.

**Linked phase:** Phase 1.1

---

## 16:30 — Fix .gitignore blocking source code

**Category:** fix
**Affected files:**
- `.gitignore`

**Summary:**
Changed `downloads/` pattern to `/downloads/` so it only ignores the root-level user downloads directory, not source code paths like `apps/kumoriya_app/lib/src/features/downloads/`.

**Linked phase:** Phase 1 (infra)

---

## 17:00 — Sync timestamp fix for watch history and library entries

**Category:** fix
**Affected files:**
- `packages/kumoriya_storage/lib/src/contracts/anime_progress_store.dart`
- `packages/kumoriya_storage/lib/src/drift/drift_anime_progress_store.dart`
- `packages/kumoriya_storage/lib/src/contracts/library_store.dart`
- `packages/kumoriya_storage/lib/src/drift/drift_library_store.dart`
- `packages/kumoriya_sync/lib/src/impl/http_sync_service.dart`
- `packages/kumoriya_sync/lib/src/models/library_sync_entry.dart`
- `apps/kumoriya_app/lib/src/shared/sync/sync_aware_progress_store.dart`
- `apps/kumoriya_app/lib/src/shared/sync/sync_aware_library_store.dart`
- 4 test fakes

**Summary:**
Fixed two root causes of broken chronological ordering after sync:
1. `upsertWatchHistory` always used `DateTime.now()` for `lastAccessedAt`, discarding the server's original timestamp.
2. `LibrarySyncEntry` model had no `addedAt` field, so the server's `added_at` was never captured.
Both store methods now accept optional timestamp params that default to `DateTime.now()` for non-sync callers, preserving backward compatibility.

**Linked phase:** Phase 2.1

---

## 17:15 — Anime relations caching in Drift

**Category:** feature
**Affected files:**
- `packages/kumoriya_storage/lib/src/drift/tables/anilist_cache_table.dart`
- `packages/kumoriya_storage/lib/src/contracts/anilist_cache_store.dart`
- `packages/kumoriya_storage/lib/src/drift/drift_anilist_cache_store.dart`
- `packages/kumoriya_storage/lib/src/drift/app_database.dart` (schema v18→v19)
- `packages/kumoriya_storage/lib/src/drift/app_database.g.dart` (regenerated)
- `apps/kumoriya_app/lib/src/features/anime_catalog/application/services/cached_anime_catalog_repository.dart`

**Summary:**
Relations (prequel, sequel, side story, etc.) were never persisted to local cache, so they disappeared during offline use or cache fallback. Added nullable `relations` TEXT column (JSON) to `anilist_cache` table. When anime detail is fetched, each related anime is also persisted to cache, and the relation mapping (anilistId + type) is serialized as JSON. On cache restore, `_restoreRelations` loads related anime by ID and reconstructs `AnimeRelation` objects.

**Linked phase:** Phase 2.2

---

## 17:30 — Catalog-level freshness caching

**Category:** feature
**Affected files:**
- `apps/kumoriya_app/lib/src/features/anime_catalog/application/services/cached_anime_catalog_repository.dart`

**Summary:**
Added in-memory `_catalogLastFetched` map that tracks the last successful network fetch per catalog query key. When a catalog is requested within its freshness window, results are served directly from Drift without hitting AniList. TTLs: home 4h, calendar 6h, season/upcoming/discovery 8h, recommendations 12h. This complements the existing GraphQL client 5-min cache and Riverpod provider keep-alive, significantly reducing API calls in long sessions.

**Linked phase:** Phase 2.3

---

## 17:35 — Smart cache cleanup on startup

**Category:** feature
**Affected files:**
- `apps/kumoriya_app/lib/main.dart`

**Summary:**
Expanded `_purgeExpiredCaches` to clean all four cache stores on app startup: source availability (7 days), AniList metadata (30 days), AniSkip skip timestamps (14 days), translation cache (30 days). Previously only source availability was being cleaned. This prevents indefinite storage growth from stale cache entries.

**Linked phase:** Phase 2.4

---
