# Dev Diary — 2026-06-15

> Auto-maintained by dev-diary skill. One entry per approved change.

---

## 03:13 — Watch Party: Manejo de Sesiones Stale + Gate de Autenticación

**Category:** fix
**Affected files:**
- `apps/kumoriya_app/lib/src/features/watch_party/application/providers/party_providers.dart`
- `apps/kumoriya_app/lib/src/features/watch_party/presentation/pages/party_anime_page.dart`
- `apps/kumoriya_app/lib/l10n/app_en.arb`
- `apps/kumoriya_app/lib/l10n/app_es.arb`

**Summary:**
Implemented 'leave-before-join' retry logic for Watch Party rooms when intercepting 409 Conflict errors (stale/ghost session), allowing users to automatically recover and join/create rooms after sudden disconnects. Integrated an authentication gate on the Watch Party page to show a custom unauthenticated message view with a redirection button to the login page for users who are not signed in.

**Linked phase:** N/A

---
## 04:08 — Watch Party: Force Leave API for Ghost Sessions

**Category:** fix
**Affected files:**
- `kumoriya-api/internal/handler/party_handler.go`
- `kumoriya-api/internal/service/party_broker.go`

**Summary:**
Implemented server-side recovery for Watch Party ghost sessions (Alternative A). Added a `ForceLeave` method to `PartyBrokerClient` that calls the internal worker recovery endpoint `/users/:userId/force-leave`. Updated the `POST /party/leave` handler in V2 to invoke `ForceLeave` when called without a `roomId` (which happens during a 409 recovery attempt by the Flutter app). This cleanly kicks the user from any stale room they might be stuck in, allowing the client's retry logic to succeed.

**Linked phase:** N/A

---
